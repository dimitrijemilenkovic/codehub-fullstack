# Database Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=codehub
PGPASSWORD=your_secure_database_password
PGDATABASE=codehub_db
DATABASE_URL=postgresql://codehub:your_secure_database_password@localhost:5432/codehub_db

# Security Configuration (CRITICAL - DO NOT USE DEFAULT VALUES IN PRODUCTION)
JWT_SECRET=your_super_secure_jwt_secret_at_least_32_characters_long
NODE_ENV=development

# Server Configuration
PORT=3001

# Frontend URL (for CORS in production)
FRONTEND_URL=https://your-production-domain.com

# Security Notes:
# 1. JWT_SECRET must be at least 32 characters long and cryptographically secure
# 2. Generate strong passwords for database
# 3. Never commit actual .env file to version control
# 4. Use different secrets for development, staging, and production
# 5. Consider using environment-specific key management systems in production